apiVersion: v1
kind: Namespace
metadata:
  name: openshift-cnv
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: virt-platform-autopilot
  namespace: openshift-cnv
  labels:
    app: virt-platform-autopilot
    control-plane: controller-manager
spec:
  selector:
    matchLabels:
      app: virt-platform-autopilot
      control-plane: controller-manager
  replicas: 1
  template:
    metadata:
      labels:
        app: virt-platform-autopilot
        control-plane: controller-manager
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: manager
          image: quay.io/tiraboschi/virt-platform-autopilot:test-descheduler-20260217-171253
          imagePullPolicy: IfNotPresent
          command:
            - /manager
          args:
            - --leader-elect
            - --namespace=openshift-cnv
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8082
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              cpu: 500m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
      serviceAccountName: virt-platform-autopilot
      terminationGracePeriodSeconds: 10
