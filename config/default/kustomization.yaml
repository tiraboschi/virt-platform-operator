apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: virt-platform-operator-system

namePrefix: virt-platform-operator-

resources:
  - ../manager
  - ../rbac

commonLabels:
  app.kubernetes.io/name: virt-platform-operator
  app.kubernetes.io/component: operator
